node_modules/
.venv*/
*.egg-info/
__pycache__/
.pytest_cache/
.DS_Store
.claude/

# runtime state and analysis output — per-target data never enters the skill repo
state/*
!state/README.md
output/*
!output/README.md

# generated exports (html report, future formats) — regenerable, never tracked
exported/

# legacy, pre-relocation analyzer-owned Go tool dir (never committed). Bootstrap does
# NOT populate this — the callgraph binary is installed manually by the developer, and
# current installs target the data-root runtime location instead (see README.md); this
# in-code path is honored only as a fallback for an already-populated legacy install.
# node_tools/node_modules is covered by the node_modules/ rule above; its pinned
# package.json + lockfile stay tracked.
wrapper/go_tools/

# 57B-30 call-graph spike node tooling (not shipped)
wrapper/node_tools_spike/
